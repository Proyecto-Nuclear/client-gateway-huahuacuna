/**
 * Enums locales para el módulo de eventos
 * (Replicados desde @prisma/client del microservicio servicios)
 */

export enum EventStatus {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
  FINISHED = 'FINISHED',
  CANCELLED = 'CANCELLED',
}
